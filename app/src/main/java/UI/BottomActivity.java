package UI;

import android.app.Activity;

public class BottomActivity extends Activity {

/*    @Nullable
    @Override
    public View onCreateView(@NonNull LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) {
        View rootView = inflater.inflate(R.layout.bottomfragment, container, false);

        // 获取帧布局中的视图组件

        Button homeButton = rootView.findViewById(R.id.homeButton);
        Button bookshelfButton = rootView.findViewById(R.id.bookshelfButton);
        Button testButton = rootView.findViewById(R.id.testButton);
        Button promotionButton = rootView.findViewById(R.id.promotionButton);
        Button myButton = rootView.findViewById(R.id.myButton);

        // 在这里添加你的其他视图组件的获取和操作


        return rootView;
    }*/
}
